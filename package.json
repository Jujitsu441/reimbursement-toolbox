{
  "name": "reimbursement-toolbox",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "preview": "npx wrangler@3 pages dev public",
    "deploy": "npx wrangler@3 pages deploy public --project-name reimbursement-toolbox --commit-dirty=true"
  },
  "devDependencies": {
    "wrangler": "^3.114.17"
  }
}
